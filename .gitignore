bin/
*.prg
!versions/*.prg

ruvector.db
.vscode/
*.log

# M8 corpus-pipeline intermediates (regenerated from the ZIM on the dev host).
scripts/m8-corpus/cached/
.cache/
.claude/scheduled_tasks.lock

# monkeyc build intermediates / MIR dumps under versions/ (regenerated by the
# compiler; not source). Release .prg archives (versions/*.prg) stay tracked.
versions/external-mir/
versions/internal-mir/
versions/gen/
versions/*.debug.xml
